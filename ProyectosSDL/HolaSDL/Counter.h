#pragma once

#include "checkML.h"
#include "Text.h"
class Counter : public GameObject, public Renderable
{/*
  private:
	int _score;
	Text *_text;
  public:
	Counter(float x, float y, int width, int height, SDL_Color color, Font *font);
	Counter(b2Vec2 position, int width, int height, SDL_Color color, Font *font);
	~Counter();
	int setScore(const int num);
	int getScore() const;
	int increaseScore(const int num);
	virtual void render() const;*/
};
